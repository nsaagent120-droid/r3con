# r3con 7.3.0 stable — Requirements
# ─────────────────────────────────────────
# La vraie version fusionnée, offline-first, explicable

# CORE (required, 100% offline)
click>=8.1.0,<9.0.0
rich>=13.0.0,<15.0.0

# Utilities (reporting, config)
pyyaml>=6.0
jinja2>=3.1.0

# Binary analysis (recommended, offline)
capstone>=5.0.0
lief>=0.13.0

# AST engine for C — arbre syntaxe réel vs regex fallback
# Optionnel : sans ces paquets, modules retombent sur chemin regex
tree-sitter>=0.21.0
tree-sitter-c>=0.21.0

# SMT solver — preuve chemin inatteignable vs heuristique
# Optionnel : sans z3, reachability="unknown"
z3-solver>=4.12.0

# Web dashboard (optional)
flask>=2.0.0

# YARA scanning (optional)
yara-python>=4.5.0

# AI providers (all optional, offline by default)
# DeepSeek / OpenAI compatible
openai>=1.3.0
# Together AI (NVIDIA Nemotron free)
together>=0.2.0

# ─────────────────────────────────────────
# Install profiles via pyproject.toml extras:
# pip install -e '.[binary]'   -> capstone, lief
# pip install -e '.[reporting]' -> jinja2, pyyaml
# pip install -e '.[web]'       -> flask, jinja2
# pip install -e '.[yara]'      -> yara-python
# pip install -e '.[ast]'       -> tree-sitter
# pip install -e '.[symbolic]'  -> z3-solver
# pip install -e '.[full]'      -> all above
# pip install -e '.[dev]'       -> pytest, ruff, black, mypy, bandit, build, twine
