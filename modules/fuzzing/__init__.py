"""Adaptateurs et outils de triage pour le lab de fuzzing r3con.

Note: ce paquet était historiquement sans ``__init__.py`` et n'était donc pas
inclus dans les roues (setuptools ``find``). Ce fichier corrige le packaging
sans modifier ``adapters.py``.
"""
